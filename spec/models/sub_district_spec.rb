require 'spec_helper'

describe SubDistrict do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    SubDistrict.create!(@valid_attributes)
  end
end
